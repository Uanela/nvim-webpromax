local number = "8"
local name = "nvim-config-check"
local plugin_name = name:gsub("-challenge", "")

return {
  -- plugin_name,
  -- dir = "~/Documents/development/lua/solutions-nvim-plugin-challgens/" .. number .. "-" .. name,
  -- config = function()
  --   require(plugin_name).setup()
  -- end
}
